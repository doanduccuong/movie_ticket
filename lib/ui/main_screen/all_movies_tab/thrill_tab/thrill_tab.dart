import 'package:flutter/material.dart';
class ThrillTab extends StatelessWidget {
  const ThrillTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
