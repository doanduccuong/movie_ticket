import 'package:flutter/cupertino.dart';

class ForKidTab extends StatelessWidget {
  const ForKidTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
